# Defect-Fix Register

This service is deliberately **not** a bug-for-bug port. The thirty-four defects catalogued in the
court-register design document (§7, C1–C34) are **fixed**, and every fix is recorded here — legacy
behaviour, fixed behaviour, the test that pins the fix, and its sign-off state. The rules:

- A fix merged without a register entry is itself the defect: revert it or register it.
- The differential audit fails the build in both directions — a catalogued defect still reproduced,
  **or** a behaviour change that is not on this register. Legacy remains the oracle for everything
  not catalogued.
- Each fix is implemented and pinned by the phase that lands it; a row moves to **FIXED** only in
  the commit whose pinning test passes. Rows marked **sign-off pending** gate **cutover**, not
  implementation: a register produced for a live court centre must not carry an unapproved content
  change.
- Legacy citations are into `cpp-context-azure-legalaidagency/azure-functions/durable-functions`
  (`$DF`) at HEAD `0d63f3ae`.
- **C1–C34 are the design document's catalogue; anything numbered above it was found later** — in
  review or in the differential audit — and appended here rather than shipped as a silent
  divergence. That is the mechanism Principle I requires of an uncatalogued behaviour change:
  it earns a C-number with review, or the legacy behaviour stands.

| # | Title | Legacy behaviour (defective) | Fixed behaviour | Rationale / impact | Pinning test | Sign-off status |
|---|---|---|---|---|---|---|
| C1 | Silent outbound POST | `$DF/ProcessOutboundCourtRegister/index.js:17-25` — the single POST to progression bypasses `AxiosRetryWrapper`; every error is caught, logged and swallowed; the response status is never inspected. A failed submission is indistinguishable from a delivered one | The submission client **throws**: transient failures (connect/IO, 5xx, 429, 408) become `RETRYING` with broker redelivery; non-transient 4xx become `FAILED` + dead-letter; `processed_output.response_code` records what progression answered. Success is **202 and nothing else** — any other 2xx is a non-transient `SUBMISSION_NOT_ACCEPTED` | The migration's reason to exist; ends the silent-loss mode (Audit FR.03). ⚠ Ops expectation: failures that were invisible become visible | `ProgressionRegisterSubmissionClientTest.a_400_is_a_recorded_failure_never_silence`; `ProgressionCommandGatewayTest.only_202_is_success` | PLANNED — internal reliability, no content change |
| C2 | Orchestration reports success on failure | `$DF/CourtRegisterOrchestrator/index.js:70-76` — the catch-all returns `{Success:false}` from a *completed* orchestration; `:71` reads `context.df.getInput()` inside the catch and can itself rethrow | Every code path ends in a recorded terminal state on `processed_request` (`COMPLETED` with a reason, `RETRYING`, or `FAILED` with a reason); there is no path that completes while reporting failure, and no failure handling that can fail unrecorded | Strictly better; no behavioural risk | `DistributionPipelineTest.every_failure_ends_in_a_recorded_terminal_state` | FIXED — internal reliability, no content change |
| C3 | Retry wrapper never retries what matters | `$DF/CommonUtility/AxiosRetryWrapper.js:19,34` — `retriesLeft <= 1 \|\| (error.response && status <= 429)` makes **every status ≤ 429 non-retryable, including 429 and 408**; fixed 1 s interval; env read once at module load | Config-driven retry policy applied identically to **all three named clients** (progression submission, reference-data, results-query payload fallback): connect/IO, 5xx, 429 and 408 retry with exponential backoff bounded by `max-backoff`; `Retry-After` honoured in **delta-seconds only, bounded** (an HTTP-date value is classified, never parsed); other 4xx never retry; settings are runtime configuration, not module-load state | Rate-limit and timeout responses are precisely the retryable ones the legacy refuses to retry | `ProgressionCommandGatewayTest.retry_after_is_bounded_and_delta_seconds_only`; `ProgressionCommandGatewayTest.four_two_nine_and_four_oh_eight_are_retryable`; `ReferenceDataNowSubscriptionsClientTest.retry_taxonomy_matches_the_submission_client`; `ResultsQueryHearingPayloadClientTest.retry_taxonomy_matches_the_submission_client` | PLANNED — internal reliability, no content change |
| C4 | Court-centre code compared against informant codes | `$DF/CourtRegisterSubscriptions/index.js:51` + `$DF/NowsHelper/service/SubscriptionsService.js:48-54` — the same `ouCode` (= `courtCentre.code`) is fed to **both** `matchCourtHouse` (`selectedCourtHouses.includes`) and `matchProsecutor` (`informantCode ===`) | `ouCode` participates in **court-house matching only**; the informant-code equality branch is not evaluated for this flow | A court-centre OU code has no business meaning as an informant code; an accidental equality would add a recipient nobody subscribed. ⚠ Content-changing **iff** any live subscription's `informantCode` equals a court-centre code — the Q9 `now_subscriptions` snapshot verifies before cutover | `SubscriptionRulesTest.ou_code_matches_court_houses_only` | FIXED — sign-off pending (content-changing, gate before cutover (business + reference-data snapshot Q9)) |
| C5 | No court-register branch in the matcher | `$DF/NowsHelper/service/SubscriptionsService.js:14-45` — no branch keys on `isCourtRegisterSubscription`; a court-register-only subscription matches only via `selectedCourtHouses`, a coincidental `informantCode`, `isNowSubscription`/`isEDTSubscription`, or `isPrisonCourtRegisterSubscription`; `matchCpsProsecuted` is dead code | An explicit court-register-aware branch: subscriptions carrying `isCourtRegisterSubscription` match via `selectedCourtHouses` plus the vocabulary/prompt/result predicates; subscriptions **not** carrying the flag cannot match this flow through the accidental routes | The upstream filter (`index.js:33-36`) already restricts candidates to `isCourtRegisterSubscription`, so in practice this pins intent rather than changing today's recipient sets — but the accidental routes must not survive the port. ⚠ Same Q9 snapshot verifies no live subscription depends on them | `SubscriptionRulesTest.does_not_match_a_subscription_that_is_not_a_court_register_one` — the negative, which is the whole fix: the legacy matches such a subscription through `isNowSubscription`, a coincidental `informantCode` or `isPrisonCourtRegisterSubscription`, and this refuses it. `SubscriptionRulesTest.court_register_subscriptions_match_via_selected_court_houses` is supporting coverage — the legacy passes it too, so on its own it pins nothing | FIXED — sign-off pending (gate before cutover (reference-data snapshot Q9)) |
| C6 | Dead empty-list guard | `$DF/SetCourtRegister/index.js:35-38` — `if (!defendantContextBaseList) return;` can never fire (the builder always returns an array); an empty list flows on as `registerDefendants: []` | The guard disappears in the rewrite; an empty defendant-context list is the explicit terminal state `COMPLETED, completion_reason = no-defendants` | "Nothing to publish" becomes a state, not an accident | `RegisterBuilderTest.no_defendants_is_a_named_outcome` (the fragment half); the chain-level pin — an empty hearing recorded as `no-defendants` through the real transformer rather than a mocked one — lands with T056 | PLANNED — pipeline half landed; completes with the transformation chain (T056). The named builder test passes with the fix reverted, because the guard it removes was already dead: what the row actually claims is that an empty defendant list reaches the processed log as `no-defendants`, and the only test asserting that today (`DistributionPipelineTest.records_a_hearing_that_gathered_nobody_as_no_defendants`) mocks the transformer and so pins the pipeline's branch rather than the fix. The row moves to FIXED when the real chain maps an empty fragment to that outcome end to end — internal reliability, no content change |
| C7 | Group-proceedings skip is silent and loosely typed | `$DF/CourtRegisterOrchestrator/index.js:21-23` — `isGroupProceedings == null \|\| == false` ⇒ proceed; any truthy value (including the string `"false"`) suppresses the register with **no record**, `Success: true` | The business skip is retained, evaluated **strictly**: only JSON boolean `true` suppresses, recorded as `COMPLETED, completion_reason = group-proceedings`. A non-boolean value is a contract anomaly: WARN log + metric, and the register is **not** suppressed | The skip is a business rule; the silence and the loose comparison are not. ⚠ Content-changing only for pathological payloads (a string `"false"` suppresses today); the producer typing question is design-doc Q8 | `DistributionPipelineTest.group_proceedings_skip_is_recorded`; `DistributionPipelineTest.only_boolean_true_suppresses_the_register` (the strictness itself in `GroupProceedingsPolicyTest.never_suppresses_on_a_value_that_is_not_a_boolean` and `.counts_every_non_boolean_value`) | FIXED — sign-off pending (content-changing edge, gate before cutover (producer/hearing team, Q8)) |
| C8 | Attendance lookup assigns instead of comparing | `$DF/OutboundCourtRegister/CourtRegisterRequest/Mappers/Hearing/HearingMapper.js:13,22` — `find(d => d.defendantId = defendantId)` (assignment): always returns element 0 of `defendantAttendance`, **mutates** its `defendantId`, throws on an empty array | The attendance record is selected by **equality** against the mapped defendant's ids (`defendantContext.defendantIds` membership); the input is never mutated; an absent or empty attendance list yields `defendantPresent = false`, no throw | With C9, `defendantPresent` and `defendantAppearanceDetails` become real data instead of noise | `HearingMapperTest.attendance_is_selected_for_the_mapped_defendant` | FIXED — pinning test passing (T054); sign-off pending (content-changing (PDF), gate before cutover (business + progression, Q5)) |
| C9 | Attendance date can never match | `$DF/…/Hearing/HearingMapper.js:14,23` — compares `attendanceDay.day` (a date, `2019-02-01`) with `registerDate` (a datetime) — **never equal in production**, so `defendantPresent` is always `false` and `defendantAppearanceDetails` always absent | Attendance is matched on the **day of the defendant's latest `judicialResult.orderedDate`** (the shared kernel's `VocabularyService.getAttendanceInfo:245-274` semantics); `defendantPresent` reflects attendance on that day; `defendantAppearanceDetails` maps all three `attendanceType` values (`In person` / `By video link` / `Not present`) | `defendantPresent` is schema-required and provably wrong on every production register today; the PDF's appearance column renders its `-` fallback as a consequence | `HearingMapperTest.defendant_present_reflects_attendance_on_the_ordered_day` | FIXED — pinning test passing (T054); sign-off pending (content-changing (PDF), gate before cutover (business + progression, Q5)) |
| C10 | BST local time labelled as UTC | `$DF/NowsHelper/service/DateService.js:36-38` — `getLocalDateTime` formats Europe/London wall-clock time then appends a literal `'Z'`: a 10:00Z share carries `registerDate` 11:00 "Z" in BST | `registerDate` and `hearingDate` carry **true instants**: `sharedTime` is preserved as the UTC instant it is; hearing dates are carried as recorded, never re-labelled through a timezone conversion | Timestamps stored by progression and printed on the register shift by one hour in BST today. ⚠ Content-changing: stored values, the filename day and the refdata lookup day (C12) all move for BST evening shares | `DatesTest.shared_time_is_never_relabelled` (the `SetCourtRegister` twin asserts 10:00Z in → 10:00Z out) | FIXED — sign-off pending (content-changing, gate before cutover (business + progression)) |
| C11 | Filename carries colons and is not unique | `$DF/OutboundCourtRegister/index.js:33` — `court-register_{registerDate}_{courtCentre.code}.pdf` embeds the full datetime (`court-register_2020-06-01T11:00:00Z_B01LY.pdf`): colons are invalid in Windows filenames and two hearings at one centre in the same second collide | `court-register_{yyyy-MM-dd}_{courtCentreCode}_{hearingId}.pdf` — the (corrected) register day, the court-centre code, and the hearing id for uniqueness; no colons | File Service metadata and anything downstream that saves the attachment sees a new name shape. ⚠ Progression-visible | `AggregationMapperTest.file_name_is_dated_coded_and_unique` | PLANNED — sign-off pending (wire-visible, gate before cutover (progression)) |
| C12 | Evening shares read the next day's subscriptions | `$DF/NowsHelper/service/ReferenceDataService.js:38` — `on = new Date(registerDate).toISOString().slice(0,10)`; because of C10's +1 h, a hearing shared 23:00–00:00 BST resolves the **next day's** subscription set | Falls out of C10: the `on` day is the UTC day of `sharedTime`, so an evening share reads the subscription set in force on the day it was shared | ⚠ Recipient-set change for BST evening shares | `DatesTest.bst_evening_share_uses_the_share_day` | FIXED — sign-off pending (content-changing, gate before cutover (business)) |
| C13 | Ordered-date parsing wrong format, throwing catch | `$DF/NowsHelper/service/RegisterFragmentService.js:30-44` — parses `YYYY-MM-DD` data with `DateService.parse`'s default `'YYYY/MM/DD'`; the `catch` calls `this.context.log` where `this` is unbound, so **the catch itself throws**, and the whole chain is swallowed upstream into `Success: true` | ISO-8601 parsing via `java.time`; a genuinely unparseable date is a classified `TransformationFailedException` (FAILED + reason), never a swallowed secondary throw | Executed by nothing in the legacy repo today (blind spot BS-02); the port constructs the case deliberately | `DatesTest.iso_dates_parse_and_failures_are_classified` | FIXED — internal reliability, no content change |
| C14 | Redis retry configuration silently dead | `$DF/HearingResultedCacheQuery/index.js:98,113-125` — node-redis **v3** `retry_strategy` passed to a **v4** client (ignored); `REDIS_MAX_RETRIES` read and never used; `:114` references an out-of-scope `hearingId` (would `ReferenceError`); env values compared string-to-number | Replaced wholesale by Lettuce client configuration with typed, validated connect/command timeouts; the dead env vars are retired (design doc Appendix A) | Not portable; superseded | `ConfigurationValidationTest` (startup validation) + `LettuceHearingPayloadCacheIT` | PLANNED — internal reliability, no content change |
| C15 | Redis TLS verification disabled | `$DF/HearingResultedCacheQuery/index.js:107` — `rejectUnauthorized: false` | Verified TLS via the platform CA bundle (Spring SSL bundles); an untrusted certificate fails the connection | Security defect; same fix the informant service shipped as its deviation 1 | `LivePayloadConfigTest.transport_security` | PLANNED — security, no content change |
| C16 | Secrets committed to source | `$DF/local.settings.json:4,5,25,27,35` — storage-account connection strings with `AccountKey=`, a Redis key, an App Insights key and a hardcoded `cjscppuid` in git | Fix by omission: Key Vault CSI + workload identity only; `system-user-id` is a required, injected secret (startup refusal when absent); nothing credential-shaped in the repo, enforced by the secrets-scanner workflow | ⚠ Rotation of the already-exposed keys is required regardless of this migration — raised to Platform/Security | `ConfigurationValidationTest.system_user_id_is_required`; secrets-scanner CI gate | PLANNED — security (key rotation tracked externally) |
| C17 | No idempotency anywhere | `$DF/CourtRegisterEventGridTrigger/index.js:21` — `startNew` with an auto-generated instance id: duplicate Event Grid deliveries start duplicate orchestrations and duplicate POSTs | `(source, requestId)` idempotency guard on the processed log: a duplicate delivery of a request already in a terminal state is settled from the recorded status without reprocessing — acknowledged if it completed, re-parked if it failed, and nothing published either way (this service has no status channel); a same-id-different-content delivery (fingerprint mismatch) dead-letters without overwriting the record | Architecturally mandatory once delivery is at-least-once. Note `progression.add-court-register` is not itself idempotent (design §3.5) — never shadow-run with sends enabled | `IdempotencyGuardIT.CompletedRequest.should_be_acknowledged_without_a_run_and_leave_the_row_alone`; `IdempotencyCollisionIT.a_colliding_request_should_be_dead_lettered_without_touching_the_record`; end to end through the real listener/guard composition in `RequestDedupeIT` (both halves, under distinct broker `messageId`s so the queue's own dedupe cannot answer for the guard); also `DeliveryExhaustionIT` and `DuplicateDetectionIT` | FIXED — architecture, no content change |
| C18 | No cutover kill-switch on the legacy triggers | `$DF/CourtRegisterEventGridTrigger` and `$DF/CourtRegisterQueueTrigger` carry no feature-flag stand-down, unlike both informant triggers | (Legacy-repo fix) A `CourtRegisterService` App Configuration flag read by both legacy triggers (parameterised `FeatureFlagClient`, fail-safe towards legacy) and by the producer's queue publisher — the single lever that moves both sides atomically | The whole cutover/rollback mechanism (design §9.1); this repo's service needs no change — it consumes only what the producer publishes | Legacy-repo twin tests of the informant `FeatureFlagClient` wiring | **PENDING — legacy-repo item**; owner: cutover story in `cpp-context-azure-legalaidagency` + `cpp-context-results` |
| C19 | Youth mapper dies on non-person defendants | `$DF/…/YouthDefendant/YouthDefendantMapper.js:32,34` — `defendants[0]` with no length check, then `personDefendant.personDetails` with no legal-entity fallback: an unmatched or legal-entity record throws, the exception is swallowed at `OutboundCourtRegister/index.js:62-64`, and the whole register is silently lost | Defensive resolution: a youth-flagged defendant whose person details cannot be resolved is **omitted from the register**, WARN-logged, and recorded as a bounded reason-code count in `processed_output.anomaly_summary` (e.g. `unresolvable-youth-defendant:1`) with metric `courtregister_transformation_anomalies_total{reason}` — deliberately NOT `TRANSFORMATION_FAILED`, which is reserved for fatal transformation errors; the register survives for the resolvable defendants. Whether a youth flag can legitimately sit on a legal entity remains design-doc Q11 | Today one bad record silently kills the whole hearing's register | `YouthDefendantMapperTest.an_unresolvable_youth_defendant_is_skipped_and_reported` | PLANNED — sign-off pending (content-changing (register produced where legacy dropped it), gate before cutover (business, Q11)) |
| C20 | Unguarded court-application lookup | `$DF/…/ProsecutionCaseOrApplication/ProsecutionCaseOrApplicationMapper.js:62-65` — `hearingJson.courtApplications.find(...)` with no array guard and no result guard: an absent array or unmatched `applicationId` throws and kills the register. The prosecution-case path was guarded by SNI-9005 (`0781bbc2`); the application path was not | The application path gains the same guard as the case path: an unresolvable application is **skipped** with a WARN log naming the id and a bounded `anomaly_summary` count (`unresolvable-application:1`) plus the anomaly metric — not `TRANSFORMATION_FAILED`; the register survives with the remaining cases/applications | Symmetry with the SNI-9005 fix; one bad reference no longer destroys the document | `ProsecutionCaseOrApplicationMapperTest.an_unresolvable_application_is_skipped_and_reported` | FIXED — pinning test passing (T054); sign-off pending (content-changing (partial register instead of none), gate before cutover (business, Q11)) |
| C21 | ASN derivation dies on legal-entity records | `$DF/…/ProsecutionCaseOrApplicationMapper.js:46-55` — `getASN` filters `d => d.personDefendant.arrestSummonsNumber` with no `d.personDefendant &&` guard: a case-defendant record carrying the register defendant's own `masterDefendantId` but no `personDefendant` throws | The informant twin's guard: records without `personDefendant` contribute no ASN and cause no throw | Same silent-total-loss class as C19/C20 | `ProsecutionCaseOrApplicationMapperTest.asn_ignores_records_without_person_defendant` | FIXED — pinning test passing (T054); sign-off pending (content-changing (register survives where legacy dropped it), gate before cutover (business, Q11)) |
| C22 | Non-prosecuting-authority applications reach the register | `$DF/…/ProsecutionCaseOrApplicationMapper.js:64-66` — the comment says *"Check if applicant is prosecutingAuthority and subject is masterDefendant"* but only the subject half is implemented, here and in the eligibility gate (`$DF/NowsHelper/service/DefendantContextBaseService.js:179-187`) | Court-application eligibility requires `subject.masterDefendant` **and** `applicant.prosecutingAuthority`, in both the context-builder gate and the mapper — what the code's own comment claims, and what the informant flow enforces | Defence-initiated and other non-prosecuting-authority applications no longer appear on the register. ⚠ The design doc calls this "the highest-value content question in the register" (Q12) | `DefendantContextBuilderTest.applications_require_a_prosecuting_authority_applicant` (the context-builder gate); `ProsecutionCaseOrApplicationMapperTest.is_not_this_registers_business` (the mapper gate) | FIXED — both sites landed and both pinned (T054). The fix has two sites — `DefendantContextBaseService.js:179-187` and `ProsecutionCaseOrApplicationMapper.js:64-66` — and the mapper now applies the same rule the builder does, so an application the builder admitted cannot reach the register through the mapper either — sign-off pending (content-changing, gate before cutover (business + progression, Q12)) |
| C23 | `verdictCode` carries a prose description | `$DF/…/Offence/OffenceMapper.js:20` — `verdictCode = ((verdict\|\|{}).verdictType\|\|{}).description`: a free-text description in a field named `verdictCode`, no `VerdictCodeMapping`, no `verdictDate` | `verdictCode` carries **`verdict.verdictType.verdictCode`** — a real field of the platform verdict model (present alongside `category`/`categoryType`/`description` in the domain fixtures) — falling back to `categoryType` when absent (live payloads have been observed carrying only `category`/`categoryType`/`id`); never the prose description | ⚠ PDF-visible: progression renders this field, so the verdict column shows a code/category-type instead of prose. What the field is *meant* to carry is design-doc Q13 — this fix makes it carry what its name says while that question is settled | `OffenceMapperTest.verdict_code_carries_the_verdict_code` | FIXED — pinning test passing (T054); sign-off pending (content-changing (PDF), gate before cutover (business + progression, Q13)) |
| C24 | `####` sentinel and `undefined` residue in wording | `$DF/…/Offence/OffenceMapper.js:17` — `wording + '####' + offenceLegislation`, yielding a literal `"…####undefined"` when legislation is absent; progression's `CourtRegisterPdfPayloadGenerator.java:336` substitutes `####` → newline at render time | `wording` and `offenceLegislation` are joined with a **real newline**, and the legislation line is omitted entirely when absent. Progression's `replaceAll("####", "\n")` passes a literal `\n` through unchanged, so the rendered payload is equivalent for the populated case and loses the `undefined` residue for the absent case | Removes a client-side rendering convention from the data and a cosmetic defect from the PDF. ⚠ Must be verified once against a rendered `OEE_Layout5` PDF before cutover | `OffenceMapperTest.wording_joins_with_a_newline_and_omits_absent_legislation` | FIXED — pinning test passing (T054); sign-off pending (wire-visible, gate before cutover (progression)) |
| C25 | Ethnicity only when both descriptions present | `$DF/…/YouthDefendant/YouthDefendantMapper.js:70-74` — returns a value only when **both** observed and self-defined ethnicity are present (then returns observed); self-defined-only yields nothing; the `\|\|` at `:72` is unreachable | `observed ?? selfDefined` — the observed description when present, otherwise the self-defined one | ⚠ Adds ethnicity data to youth-defendant registers that previously omitted it: a **PII disclosure change** requiring information-governance sign-off, not just business sign-off (design-doc Q15) | `YouthDefendantMapperTest.ethnicity_prefers_observed_and_falls_back_to_self_defined` | PLANNED — sign-off pending (content-changing, gate before cutover (business + IG, Q15)) |
| C26 | Mapper/model drift | `$DF/…/CourtRegisterRequest/Models/*` — `arrestSummonsNumbers` (plural) declared, singular written; five declared-never-populated fields (`prosecutorName`, `applicationDecision(+Date)`, `applicationResponse(+Date)`); `hearingDate`/`hearingId`/`wording`/`courtApplicationId` written by mappers but absent from the models; two dead methods; unused lodash import | Typed Java records validated against the **vendored progression schemas** (`src/main/resources/contracts/progression/`, `criminal-court-public-model` 17.103.13): singular `arrestSummonsNumber`, every written field modelled, no dead fields or methods | The wire shape today is defined by the mappers, which the port reproduces — so no output change; the *model* becomes honest. `prosecutorName` and the four `application*` fields are flagged to the business (progression's PDF generator expects a `prosecutorName` concept) | `OutboundContractValidationTest.records_match_the_vendored_schemas` | PLANNED — **fragment half landed** (T036): the register fragment declares and writes `courtCentreId`, spelled the way the payload spells it, pinned by `RegisterBuilderTest.carries_the_court_centre_id_spelled_correctly`. The row moves to FIXED with the outbound record family and its named test, in the mapper phase — internal, no content change |
| C27 | Recipients dropped without a trace | `$DF/…/Recipient/RecipientMapper.js:9-34` — letter-delivery subscriptions (`firstClassLetterDelivery`/`secondClassLetterDelivery`) are dropped without even a log line (informant at least logs them); a recipient with no usable `emailAddress1` — absent, an explicit JSON `null` (which the legacy's `!== undefined` check wrongly keeps, producing a document the frozen contract refuses), or whitespace-only — is silently dropped (or, for the explicit-null shape, wrongly kept); `emailAddress2` is carried and never used | Email-only delivery is **retained** (product behaviour); every drop — letter-delivery subscription, unusable `emailAddress1` (absent, explicit `null`, or whitespace-only — one rule for all three), `emailDelivery`/`forDistribution` false — is WARN-logged and counted in `processed_output.anomaly_summary` (`letter-delivery-dropped:n`, `recipient-missing-email:n`, `recipient-not-for-distribution:n`) with the anomaly metric; `emailAddress2` continues to be carried (the informant R3 open item) | No recipient-set change; the drops stop being invisible | `RecipientMapperTest.every_dropped_recipient_is_logged_and_counted` | PLANNED — internal reliability, no content change |
| C28 | A mapper test file Jest never runs | `$DF/…/Mappers/Address/test/AddressMapperTest.js` — filename misses Jest's `*.test.js` pattern so it has **never executed**; its first case asserts the wrong value (`[]` where the mapper returns `undefined`) and its second references an undefined `Address` symbol. The identical dead file exists in the PrisonCourtRegister tree | (Legacy-repo fix) Rename to `AddressMapper.test.js` and repair both cases, in **both** the CourtRegister and PrisonCourtRegister trees. In this repo the two cases are repaired as real tests regardless: absent input ⇒ `null` result; populated input ⇒ address1–5 pass-through **and** the `postcode` → `postCode` case change | Until renamed, the legacy suite reports coverage it does not have | `AddressMapperTest.absent_address_maps_to_nothing`; `AddressMapperTest.postcode_case_changes_on_the_way_out` (this repo) | **PENDING — legacy-repo item**; owner: `cpp-context-azure-legalaidagency` maintainers, next change to that tree |
| C29 | Missing required address silently loses the register | Progression's schemas require `courtRegisterDefendant.address`, `courtRegisterParentGuardian.address` and `courtRegisterAddress.address1` (`additionalProperties: false`), but `$DF/…/Mappers/Address/AddressMapper.js:6-8` returns `undefined` for an absent address ⇒ progression answers 400 ⇒ C1 swallows it ⇒ **the whole hearing's register is lost with no trace** | The outbound document is **validated against the vendored contract before sending**; a schema-invalid document is an explicit `FAILED` with a bounded reason and a dead-letter — visible, quantifiable, replayable. The business fallback for a genuinely address-less youth defendant (refuse vs placeholder) is design-doc Q4 and gates cutover | The strongest single business argument for the migration — the same class the informant replay proved live (CRA-232) | `OutboundContractValidationTest.a_missing_required_address_is_an_explicit_failure` | PLANNED — sign-off pending (fallback decision, gate before cutover (business, Q4); the visibility fix itself is internal) |
| C30 | Major-creditor flags match inconsistently on empty data | `$DF/NowsHelper/service/VocabularyService.js:329-334` — the two-arg construction leaves the major-creditor lists `[]` forever; `$DF/NowsHelper/service/SubscriptionsService.js:275-298` then makes the consequences **asymmetric**: `prosecutorMajorCreditor`/`nonProsecutorMajorCreditor` can never match (`.length > 0` required) while `anyMajorCreditor` **always passes** (`!= null`; an empty array is not null) | Consistent semantics: all three flags require a **non-empty** applicable major-creditor list, so a major-creditor-flagged subscription never matches a court register — consistently. (Major creditors are a NOWs/informant concept; the court register has none) | ⚠ Content-changing **iff** any live court-register subscription relies on the vacuously-true `anyMajorCreditor` — the Q9 snapshot verifies. The informant flow pinned this asymmetry as oddity `o02` ("Same emptiness, opposite answers") | `SubscriptionRulesTest.major_creditor_flags_never_match_a_court_register` | FIXED — both halves landed: the vocabulary carries the two lists present-and-empty rather than dropping them (T034, `VocabularyBuilderTest.keeps_them_present_and_empty_rather_than_absent`), and the three predicates now agree about what an empty list means (T037, the named test plus `SubscriptionRulesTest.neither_specific_creditor_flag_matches_one` and `.leaves_a_subscription_that_asks_for_no_creditor_alone`). The legacy's per-value `FCOMP`/`CREDITOR_NAME` scan is not ported: it is unreachable behind the same emptiness test — sign-off pending (gate before cutover (reference-data snapshot Q9)) |
| C31 | Only the first defendant's vocabulary is matched | `$DF/CourtRegisterSubscriptions/index.js:49` — `subscriptionObj.vocabulary = registerDefendants[0].vocabulary`: one register can carry many defendants, but matching runs on the first defendant's vocabulary only — and the list is **not** pre-filtered to youths, so an adult-first hearing matches on adult vocabulary for a youth register | Subscription matching is evaluated **per defendant**: a subscription matches if **any** register defendant's vocabulary satisfies its predicates (`judicialResults` remain collected across all defendants, as today) | ⚠ Recipient-set change: youth-keyed subscriptions now match hearings where the youth is not defendant[0] — today those registers can silently reach nobody (design-doc Q16) | `SubscriptionMatcherTest.any_defendants_vocabulary_can_match` | FIXED — sign-off pending (content-changing, gate before cutover (business, Q16)) |
| C32 | Cache-and-fallback double miss is silent | `$DF/HearingResultedCacheQuery/index.js:50-56` — an empty query-API body returns `undefined` (no `else`), an error returns `null`; both make the orchestrator's `if (hearingResultedObj)` false ⇒ silent, unrecorded stop reported as `Success: true` | Cache miss **and** fallback miss is a **TRANSIENT, retryable failure with a bounded reason** (the payload may simply not be written yet); an unreachable cache is a *miss*, scoped to `RedisException`, so the query fallback still gets its turn; empty-body and error are distinguished in the reason | A register that was never buildable stops masquerading as a delivered one | `CachedHearingPayloadAdapterTest.a_double_miss_is_transient_never_silent` | PLANNED — **pipeline half landed** (T039): a payload neither source could supply is a recorded TRANSIENT failure with a bounded reason and a redelivered message, never a silent stop reported as success, pinned by `DistributionPipelineTest.retries_a_payload_the_cache_and_fallback_could_not_supply` and `.does_not_transform_a_hearing_it_could_not_read`. The row moves to FIXED with the adapter half — the RedisException-scoped absorb that still lets the query fallback take its turn, and the empty-body/error distinction in the reason — and its named test, in the adapter phase; internal reliability, no content change |
| C33 | All no-op outcomes are indistinguishable | `$DF/OutboundCourtRegister/index.js:17-26` (+ the orchestrator's four silent guards) — "no matched subscriptions" and "no youth defendants", the flow's two most common legitimate outcomes, both surface as a bare `null` funnelled into `Success: true` | Four bounded completion reasons — `group-proceedings`, `no-defendants`, `no-subscriptions`, `no-youth-defendants` — plus `submitted`, each a distinct `processed_request` terminal state with a metric | Essential for the cutover reconciliation. ⚠ Ops expectation: a high COMPLETED-but-not-submitted rate is *normal* for this flow | `DistributionPipelineTest.no_op_outcomes_are_distinguishable` | PLANNED — **pipeline half landed** (T039): all five completion reasons are distinct recorded terminal states with their own counter series, and the group-proceedings skip that the legacy records nothing for is one of them — pinned by the named test and by `DistributionPipelineTest.group_proceedings_skip_is_recorded`. Three of the four no-op reasons are *chosen* by the transformation, which is unwritten, so the row moves to FIXED with the mapper half (T054); internal reliability, no content change |
| C34 | Test fixtures ship in the deployed zip | `courtregister-azure-functions/courtregister-assembly.xml:98-111` — no `**/test/**` exclusion, so defendant-shaped test JSON deploys to production; the same applies to all seven function-app assemblies | (Legacy-repo fix) Add the `**/test/**` exclusion to the assemblies of the apps that are not yet migrating. This repo's Boot build packages nothing test-shaped by construction | A data-hygiene defect in apps this migration retires only one of | n/a in this repo (bootJar packaging is asserted by the build) | **PENDING — legacy-repo item**; decision owner: function-app estate owners (platform / `cpp-context-azure-legalaidagency` maintainers); trigger: the next release of any non-migrating app from the shared assembly |
| C35 | Hearing date stamped from the wall clock | `$DF/NowsHelper/service/RegisterFragmentService.js:46-55` — `getHearingDate` has two legs that end at `dateService.getLocalDateTime(undefined)`, and `moment.tz(undefined, zone)` is the current time. **(a)** A hearing whose gathered results name no ordered date matches no sitting day and falls through to `getLocalDateTime(orderedDate)` with `orderedDate` undefined. **(b)** A sitting record carrying no `sittingDay` has `getLocalDate(undefined)` formatted as *today*, so on the day a hearing's results were ordered that record matches `find` and the hearing date becomes `getLocalDateTime(hearingDay.sittingDay)` — undefined again. Either way `hearingDate` is whenever the function app happened to run, and two replays of one hearing produce two different registers | Deterministic and clock-free, because the transformation is pure (constitution Principle V). **(a)** A hearing with no ordered date carries **no `hearingDate` at all** — the same answer the legacy gives a hearing that lists no sitting days, and one the pre-send contract validation (C29) will reject explicitly if progression requires the field, rather than accept a clock reading. **(b)** A sitting record naming no day **matches nothing**, so the hearing date falls back to the ordered date — what the register carries on every day but the ordered one today. Neither leg is a transformation failure; both are ordinary answers. Separately, and in the opposite direction, the two shapes the legacy's own dereference **throws** on — a truthy `hearingDays` that is not an array (`.find` is not a function) and a `null` sitting record (`hearingDay.sittingDay` is a `TypeError`) — are classified `TransformationFailedException`, FAILED and dead-lettered: the legacy loses the whole hearing's register there, and iterating them safely would send one it never sends | ⚠ Content-changing: `hearingDate` is stored by progression and printed on the register. What it changes from is a non-reproducible clock reading, so nothing downstream can be relying on the current value meaning anything — but the field moves, for (a) to absent and for (b) to the ordered date. Uncatalogued when found in review (2026-09-01) and appended here rather than left as a silent divergence, which is what the register's append-only mechanism is for (Principle I) | `DatesTest.has_no_hearing_date_where_the_legacy_stamps_the_wall_clock`; `DatesTest.a_sitting_record_naming_no_day_matches_nothing`; the reach half in `DatesTest.refuses_a_hearing_days_that_is_not_a_list`, `DatesTest.refuses_a_sitting_record_that_is_null` and the control `DatesTest.reads_a_sitting_record_that_names_no_day` | Sign-off pending — content-affecting, gate before cutover (business) |
| C36 | Recipient-less registers submitted to nobody | `$DF/OutboundCourtRegister/index.js:28-40` + `…/Recipient/RecipientMapper.js:31-33` — when every matched subscription is dropped by the recipient predicate, `recipients` is `undefined` and the document is POSTed anyway. Progression stores it, renders the PDF at 18:00, then `CourtCentreAggregate` emits `CourtRegisterNotificationIgnored`, which nothing subscribes to (progression defect P1, design doc §3.4): the register sticks at GENERATED forever, visible to nobody | A register whose every matched recipient was dropped is **not submitted**; the run completes with the `no-subscriptions` reason — there is nobody to distribute to — and each drop is WARN-logged and counted in `processed_output.anomaly_summary` per C27. A register with at least one surviving recipient is unaffected | Content-affecting for progression: it no longer receives recipient-less rows, which today can never be delivered and only accumulate at GENERATED. Found at the mapper red phase (T051, 2026-09-01) and appended here rather than left as a silent divergence — C27 authorises the counting, not the non-submission | `AggregationMapperTest.every_matched_subscription_was_dropped` | Sign-off pending — content-affecting, gate before cutover (business + progression) |

Entries below this line are added per change, newest first. Renumber never; append only.
