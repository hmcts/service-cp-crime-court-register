<configuration>
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <fieldName>timestamp</fieldName>
                    <pattern>yyyy-MM-dd' 'HH:mm:ss.SSS</pattern>
                </timestamp>
                <!-- Lets keep the message to the left so its easier to find without scrolling -->
                <message/>
                <loggerName/>
                <threadName/>
                <logLevel/>
                <!-- MDC after the fixed columns so timestamp leads every line in a terminal -->
                <mdc/>
                <!-- The exception report's kv(...) arguments, as top-level fields: without this
                     they are rendered into the message and every saved query needs parse() -->
                <arguments/>
                <pattern>
                    <pattern>{"exception": "%xException{full}"}</pattern>
                </pattern>
            </providers>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="JSON"/>
    </root>
</configuration>